import React from "react";
import Dashboard from "../components/Dashboard"; // Import the Dashboard component

const Home = () => {
  return (
    <div>
      <Dashboard /> {/* Render the Dashboard inside Home */}
    </div>
  );
};

export default Home;
